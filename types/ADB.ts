/** 
** Описание модели устройства ABD
* @interface device
* @property {string} id - уникальный идентификатор устройства
* @property {string} type - тип устройства
*/
export interface Adb_device {
  id: string
  type: string
}
